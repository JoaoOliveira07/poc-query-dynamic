<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>POC Dynamic Query - Admin Panel</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
        }

        header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }

        header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        header p {
            font-size: 1.1em;
            opacity: 0.9;
        }

        .info-box {
            background: #f8f9fa;
            border-left: 4px solid #667eea;
            padding: 15px 20px;
            margin: 20px 30px;
            border-radius: 5px;
        }

        .info-box h3 {
            color: #667eea;
            margin-bottom: 10px;
        }

        .info-box ul {
            margin-left: 20px;
            line-height: 1.8;
        }

        .tabs {
            display: flex;
            background: #f8f9fa;
            border-bottom: 2px solid #dee2e6;
            padding: 0 30px;
        }

        .tab {
            padding: 15px 30px;
            cursor: pointer;
            border: none;
            background: transparent;
            font-size: 16px;
            font-weight: 600;
            color: #6c757d;
            transition: all 0.3s;
            border-bottom: 3px solid transparent;
            position: relative;
            top: 2px;
        }

        .tab:hover {
            color: #667eea;
            background: rgba(102, 126, 234, 0.1);
        }

        .tab.active {
            color: #667eea;
            border-bottom-color: #667eea;
            background: white;
        }

        .tab-content {
            display: none;
            padding: 30px;
        }

        .tab-content.active {
            display: block;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid #e9ecef;
        }

        .section-header h2 {
            color: #333;
            font-size: 1.8em;
        }

        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        .btn-success {
            background: #28a745;
            color: white;
        }

        .btn-success:hover {
            background: #218838;
            transform: translateY(-2px);
        }

        .btn-danger {
            background: #dc3545;
            color: white;
        }

        .btn-danger:hover {
            background: #c82333;
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: #6c757d;
            color: white;
        }

        .btn-secondary:hover {
            background: #5a6268;
        }

        .btn-small {
            padding: 6px 12px;
            font-size: 12px;
            margin: 0 3px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #495057;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 2px solid #e9ecef;
            border-radius: 5px;
            font-size: 14px;
            transition: border-color 0.3s;
        }

        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: #667eea;
        }

        .form-group textarea {
            min-height: 120px;
            font-family: 'Courier New', monospace;
            resize: vertical;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .form-actions {
            display: flex;
            gap: 10px;
            margin-top: 25px;
            padding-top: 20px;
            border-top: 2px solid #e9ecef;
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: 1000;
            animation: fadeIn 0.3s;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal.active {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background: white;
            padding: 30px;
            border-radius: 10px;
            max-width: 700px;
            width: 90%;
            max-height: 90vh;
            overflow-y: auto;
            animation: slideUp 0.3s;
        }

        @keyframes slideUp {
            from {
                transform: translateY(50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid #e9ecef;
        }

        .modal-header h3 {
            color: #333;
            font-size: 1.5em;
        }

        .close {
            font-size: 28px;
            cursor: pointer;
            color: #6c757d;
            transition: color 0.3s;
        }

        .close:hover {
            color: #dc3545;
        }

        .table-container {
            overflow-x: auto;
            margin-top: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: white;
        }

        table thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        table th,
        table td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #e9ecef;
        }

        table th {
            font-weight: 600;
            text-transform: uppercase;
            font-size: 13px;
            letter-spacing: 0.5px;
        }

        table tbody tr {
            transition: background 0.3s;
        }

        table tbody tr:hover {
            background: #f8f9fa;
        }

        .badge {
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .badge-active {
            background: #d4edda;
            color: #155724;
        }

        .badge-inactive {
            background: #f8d7da;
            color: #721c24;
        }

        .code-preview {
            background: #282c34;
            color: #abb2bf;
            padding: 20px;
            border-radius: 8px;
            font-family: 'Courier New', monospace;
            font-size: 14px;
            overflow-x: auto;
            margin: 20px 0;
            white-space: pre-wrap;
            word-wrap: break-word;
        }

        .alert {
            padding: 15px 20px;
            border-radius: 5px;
            margin: 15px 0;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border-left: 4px solid #28a745;
        }

        .alert-error {
            background: #f8d7da;
            color: #721c24;
            border-left: 4px solid #dc3545;
        }

        .alert-info {
            background: #d1ecf1;
            color: #0c5460;
            border-left: 4px solid #17a2b8;
        }

        .preview-section {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 8px;
            margin-top: 25px;
        }

        .preview-section h4 {
            color: #333;
            margin-bottom: 15px;
        }

        .results-table {
            max-height: 400px;
            overflow: auto;
            margin-top: 15px;
        }

        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top-color: white;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .checkbox-group input[type="checkbox"] {
            width: auto;
            margin: 0;
        }

        .help-text {
            font-size: 12px;
            color: #6c757d;
            margin-top: 5px;
            font-style: italic;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #6c757d;
        }

        .empty-state svg {
            width: 80px;
            height: 80px;
            margin-bottom: 20px;
            opacity: 0.5;
        }

        @media (max-width: 768px) {
            .form-row {
                grid-template-columns: 1fr;
            }

            .tabs {
                overflow-x: auto;
            }

            .tab {
                white-space: nowrap;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🔍 Dynamic Query Admin Panel</h1>
            <p>Configure and manage your dynamic queries with ease</p>
        </header>

        <div class="info-box">
            <h3>📚 Como Usar Este Painel</h3>
            <ul>
                <li><strong>Query Base:</strong> Define a query SQL base. Use placeholders como <code>{{filters}}</code> e <code>{{orders}}</code> onde os fragmentos dinâmicos serão inseridos.</li>
                <li><strong>Query Filter:</strong> Cria fragmentos de filtro reutilizáveis. Use <code>:parameterName</code> para parâmetros nomeados.</li>
                <li><strong>Query Order:</strong> Define fragmentos de ordenação (ex: <code>ORDER BY name ASC</code>).</li>
                <li><strong>Query Preview:</strong> Teste suas queries antes de salvar para ver como ficará a SQL final e visualizar resultados de exemplo.</li>
            </ul>
        </div>

        <div class="tabs">
            <button class="tab active" onclick="showTab('query-base', this)">📋 Query Base</button>
            <button class="tab" onclick="showTab('query-filter', this)">🔍 Query Filter</button>
            <button class="tab" onclick="showTab('query-order', this)">📊 Query Order</button>
            <button class="tab" onclick="showTab('query-preview', this)">🎯 Query Preview</button>
            <button class="tab" onclick="showTab('documentation', this)">📖 Documentation</button>
        </div>

        <!-- Query Base Tab -->
        <div id="query-base" class="tab-content active">
            <div class="section-header">
                <h2>Query Base Management</h2>
                <button class="btn btn-primary" onclick="openModal('queryBaseModal')">+ New Query Base</button>
            </div>
            <div class="table-container">
                <table id="queryBaseTable">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Context</th>
                            <th>Description</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="queryBaseTableBody">
                        <tr>
                            <td colspan="5" class="empty-state">
                                <div>Loading...</div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Query Filter Tab -->
        <div id="query-filter" class="tab-content">
            <div class="section-header">
                <h2>Query Filter Management</h2>
                <button class="btn btn-primary" onclick="openModal('queryFilterModal')">+ New Query Filter</button>
            </div>
            <div class="table-container">
                <table id="queryFilterTable">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Context</th>
                            <th>Parameter Key</th>
                            <th>Parameter Key</th>
                            <th>Parameter Key</th>
                            <th>Description</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                            <td colspan="6" class="empty-state">
                            <td colspan="6" class="empty-state">
                            <td colspan="6" class="empty-state">
                                <div>Loading...</div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Query Order Tab -->
        <div id="query-order" class="tab-content">
            <div class="section-header">
                <h2>Query Order Management</h2>
                <button class="btn btn-primary" onclick="openModal('queryOrderModal')">+ New Query Order</button>
            </div>
            <div class="table-container">
                <table id="queryOrderTable">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Context</th>
                            <th>Description</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="queryOrderTableBody">
                        <tr>
                            <td colspan="5" class="empty-state">
                                <div>Loading...</div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Query Preview Tab -->
        <div id="query-preview" class="tab-content">
            <div class="section-header">
                <h2>Query Preview & Test</h2>
            </div>

            <div class="form-group">
                <label>Base Query</label>
                <textarea id="previewBaseQuery" placeholder="SELECT * FROM customers WHERE 1=1 {{filters}} {{orders}}"></textarea>
                <div class="help-text">Use {{filters}} e {{orders}} como placeholders</div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label>Filter Fragments (one per line)</label>
                    <textarea id="previewFilters" placeholder="AND name LIKE :name&#10;AND email = :email"></textarea>
                    <div class="help-text">Um fragmento por linha</div>
                </div>

                <div class="form-group">
                    <label>Order Fragments (one per line)</label>
                    <textarea id="previewOrders" placeholder="ORDER BY name ASC&#10;, created_at DESC"></textarea>
                    <div class="help-text">Um fragmento por linha</div>
                </div>
            </div>

            <div class="form-group">
                <label>Parameters (JSON)</label>
                <textarea id="previewParameters" placeholder='{"name": "%John%", "email": "john@example.com"}'></textarea>
                <div class="help-text">Formato JSON com os parâmetros da query</div>
            </div>

            <div class="form-actions">
                <button class="btn btn-primary" onclick="previewQuery()">🎯 Preview Query</button>
            </div>

            <div id="previewResults"></div>
        </div>

        <!-- Documentation Tab -->
        <div id="documentation" class="tab-content">
            <div class="section-header">
                <h2>Documentation</h2>
            </div>

            <div style="line-height: 1.8; color: #333;">
                <h3 style="color: #667eea; margin-top: 30px;">🎯 Visão Geral</h3>
                <p>Este sistema permite criar queries SQL dinâmicas e reutilizáveis através de três componentes principais:</p>

                <h3 style="color: #667eea; margin-top: 30px;">📋 Query Base</h3>
                <p>A Query Base é a estrutura principal da sua consulta SQL. Ela define o SELECT principal e pode incluir placeholders para filtros e ordenações dinâmicas.</p>

                <h4 style="margin-top: 20px;">Exemplo:</h4>
                <div class="code-preview">SELECT id, name, email, created_at
FROM customers
WHERE deleted_at IS NULL {{filters}} {{orders}}</div>

                <p><strong>Placeholders disponíveis:</strong></p>
                <ul>
                    <li><code>{{filters}}</code> - Será substituído pelos filtros selecionados</li>
                    <li><code>{{orders}}</code> - Será substituído pelas ordenações selecionadas</li>
                </ul>

                <h3 style="color: #667eea; margin-top: 30px;">🔍 Query Filter</h3>
                <p>Os filtros são fragmentos SQL reutilizáveis que podem ser aplicados a diferentes queries. Use parâmetros nomeados para valores dinâmicos.</p>

                <h4 style="margin-top: 20px;">Exemplos:</h4>
                <div class="code-preview">-- Filtro por nome
AND name LIKE :name

-- Filtro por email exato
AND email = :email

-- Filtro por data de criação
AND created_at >= :startDate AND created_at <= :endDate</div>
                <p><strong>Parameter Key:</strong> Define qual parâmetro este filtro espera receber (ex: "name", "email", "startDate")</p>

                <h3 style="color: #667eea; margin-top: 30px;">📊 Query Order</h3>
                <p>Define a ordenação dos resultados. Pode incluir múltiplas colunas.</p>

                <h4 style="margin-top: 20px;">Exemplos:</h4>
                <div class="code-preview">-- Ordenar por nome ascendente
ORDER BY name ASC

-- Ordenar por data de criação descendente
ORDER BY created_at DESC

-- Ordenação múltipla
ORDER BY name ASC, created_at DESC</div>

                <h3 style="color: #667eea; margin-top: 30px;">🎯 Query Preview</h3>
                <p>Use o Query Preview para testar suas queries antes de aplicá-las. Você pode:</p>
                <ul>
                    <li>Ver a query SQL final que será executada</li>
                    <li>Verificar se há erros de sintaxe</li>
                    <li>Visualizar resultados de exemplo</li>
                    <li>Testar diferentes combinações de filtros e ordenações</li>
                </ul>

                <h3 style="color: #667eea; margin-top: 30px;">💡 Melhores Práticas</h3>
                <ul>
                    <li><strong>Context:</strong> Use contextos para organizar queries relacionadas (ex: "customer", "order", "product")</li>
                    <li><strong>Name Unique:</strong> Use nomes descritivos e únicos para facilitar a identificação</li>
                    <li><strong>Description:</strong> Documente o propósito de cada query, filtro e ordenação</li>
                    <li><strong>Active:</strong> Desative temporariamente queries sem precisar deletá-las</li>
                    <li><strong>Parameters:</strong> Use parâmetros nomeados (:paramName) para evitar SQL injection</li>
                    <li><strong>Testing:</strong> Sempre teste no Query Preview antes de usar em produção</li>
                </ul>

                <h3 style="color: #667eea; margin-top: 30px;">🔧 Exemplo Completo</h3>
                <p><strong>1. Criar Query Base:</strong></p>
                <div class="code-preview">Name: customer_search
Context: customer
Query: SELECT * FROM customers WHERE deleted_at IS NULL {{filters}} {{orders}}
Description: Base query for customer search</div>

                <p><strong>2. Criar Query Filters:</strong></p>
                <div class="code-preview">Name: filter_by_name
Context: customer
Parameter Key: name
---
Name: filter_by_email
Fragment: AND email = :email</div>
Parameter Key: email</div>

                <p><strong>3. Criar Query Order:</strong></p>
                <div class="code-preview">Name: order_by_name
Context: customer
Fragment: ORDER BY name ASC</div>

                <p><strong>4. Usar no Preview:</strong></p>
                <div class="code-preview">Base Query: SELECT * FROM customers WHERE deleted_at IS NULL {{filters}} {{orders}}
Filters: AND name LIKE :name
Orders: ORDER BY name ASC
Parameters: {"name": "%John%"}</div>

                <p><strong>Resultado:</strong></p>
                <div class="code-preview">SELECT * FROM customers WHERE deleted_at IS NULL AND name LIKE :name ORDER BY name ASC</div>
            </div>
        </div>
    </div>

    <!-- Query Base Modal -->
    <div id="queryBaseModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 id="queryBaseModalTitle">New Query Base</h3>
                <span class="close" onclick="closeModal('queryBaseModal')">&times;</span>
            </div>
            <form id="queryBaseForm" onsubmit="saveQueryBase(event)">
                <input type="hidden" id="queryBaseId">

                <div class="form-row">
                    <div class="form-group">
                        <label>Name Unique *</label>
                        <input type="text" id="queryBaseName" required placeholder="customer_search_base">
                    </div>
                    <div class="form-group">
                        <label>Context *</label>
                        <input type="text" id="queryBaseContext" required placeholder="customer">
                    </div>
                </div>

                <div class="form-group">
                    <label>Query *</label>
                    <textarea id="queryBaseQuery" required placeholder="SELECT * FROM customers WHERE deleted_at IS NULL {{filters}} {{orders}}"></textarea>
                    <div class="help-text">Use {{filters}} e {{orders}} como placeholders</div>
                </div>

                <div class="form-group">
                    <label>Description</label>
                    <textarea id="queryBaseDescription" placeholder="Describe the purpose of this query base"></textarea>
                </div>

                <div class="form-group checkbox-group">
                    <input type="checkbox" id="queryBaseActive" checked>
                    <label for="queryBaseActive" style="margin: 0;">Active</label>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-success">Save</button>
                    <button type="button" class="btn btn-secondary" onclick="closeModal('queryBaseModal')">Cancel</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Query Filter Modal -->
    <div id="queryFilterModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 id="queryFilterModalTitle">New Query Filter</h3>
                <span class="close" onclick="closeModal('queryFilterModal')">&times;</span>
            </div>
            <form id="queryFilterForm" onsubmit="saveQueryFilter(event)">
                <input type="hidden" id="queryFilterId">

                <div class="form-row">
                    <div class="form-group">
                        <label>Name Unique *</label>
                        <input type="text" id="queryFilterName" required placeholder="filter_by_name">
                    </div>
                    <div class="form-group">
                        <label>Context *</label>
                        <input type="text" id="queryFilterContext" required placeholder="customer">
                    </div>
                </div>

                <div class="form-group">
                    <label>Query Fragment *</label>
                    <textarea id="queryFilterFragment" required placeholder="AND name LIKE :name"></textarea>
                    <div class="help-text">Use :parameterName para parâmetros nomeados</div>
                </div>

                <div class="form-group">
                <div class="form-group">
                    <label>Description</label>
                    <textarea id="queryFilterDescription" placeholder="Describe what this filter does"></textarea>
                </div>

                <div class="form-group checkbox-group">
                    <input type="checkbox" id="queryFilterActive" checked>
                    <label for="queryFilterActive" style="margin: 0;">Active</label>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-success">Save</button>
                    <button type="button" class="btn btn-secondary" onclick="closeModal('queryFilterModal')">Cancel</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Query Order Modal -->
    <div id="queryOrderModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 id="queryOrderModalTitle">New Query Order</h3>
                <span class="close" onclick="closeModal('queryOrderModal')">&times;</span>
            </div>
            <form id="queryOrderForm" onsubmit="saveQueryOrder(event)">
                <input type="hidden" id="queryOrderId">

                <div class="form-row">
                    <div class="form-group">
                        <label>Name Unique *</label>
                        <input type="text" id="queryOrderName" required placeholder="order_by_name_asc">
                    </div>
                    <div class="form-group">
                        <label>Context *</label>
                        <input type="text" id="queryOrderContext" required placeholder="customer">
                    </div>
                </div>

                <div class="form-group">
                    <label>Query Fragment *</label>
                    <textarea id="queryOrderFragment" required placeholder="ORDER BY name ASC"></textarea>
                    <div class="help-text">Ex: ORDER BY column_name ASC/DESC</div>
                </div>

                <div class="form-group">
                    <label>Description</label>
                    <textarea id="queryOrderDescription" placeholder="Describe this ordering"></textarea>
                </div>

                <div class="form-group checkbox-group">
                    <input type="checkbox" id="queryOrderActive" checked>
                    <label for="queryOrderActive" style="margin: 0;">Active</label>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-success">Save</button>
                    <button type="button" class="btn btn-secondary" onclick="closeModal('queryOrderModal')">Cancel</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        const API_BASE_URL = '/api/admin/queries';

        // Tab Management
        function showTab(tabId, clickedElement) {
            console.log('showTab called with:', tabId, clickedElement);

            // Hide all tabs
            document.querySelectorAll('.tab-content').forEach(content => {
                content.classList.remove('active');
            });
            document.querySelectorAll('.tab').forEach(tab => {
                tab.classList.remove('active');
            });

            // Show selected tab
            const tabContent = document.getElementById(tabId);
            if (tabContent) {
                tabContent.classList.add('active');
                console.log('Tab activated:', tabId);
            } else {
                console.error('Tab not found:', tabId);
            }

            if (clickedElement) {
                clickedElement.classList.add('active');
            }

            // Load data for the selected tab
            if (tabId === 'query-base') loadQueryBases();
            if (tabId === 'query-filter') loadQueryFilters();
            console.log('openModal called:', modalId, editData);
            if (tabId === 'query-order') loadQueryOrders();
            if (!modal) {
                console.error('Modal not found:', modalId);
                return;
            }
        }

        // Modal Management
        function openModal(modalId, editData = null) {
            const modal = document.getElementById(modalId);
            modal.classList.add('active');

            if (editData) {
                if (modalId === 'queryBaseModal') {
                    const form = document.getElementById('queryBaseForm');
                    if (form) form.reset();
                    document.getElementById('queryBaseModalTitle').textContent = 'New Query Base';
                    document.getElementById('queryBaseId').value = '';
                }
                if (modalId === 'queryFilterModal') {
            console.log('loadQueryBases called');
            console.log('loadQueryBases called');
                    const form = document.getElementById('queryFilterForm');
                console.log('Response status:', response.status);

                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }

                    if (form) form.reset();
                console.log('Query bases loaded:', data);
                console.log('Response status:', response.status);

                if (!response.ok) {
                const tbody = document.getElementById('queryBaseTableBody');
                if (tbody) {
                    tbody.innerHTML = `<tr><td colspan="5" class="empty-state"><div style="color: red;">Error loading data: ${error.message}</div></td></tr>`;
                }
                }

                    document.getElementById('queryFilterModalTitle').textContent = 'New Query Filter';
                console.log('Query bases loaded:', data);
                    document.getElementById('queryFilterId').value = '';
                }
                if (modalId === 'queryOrderModal') {
                const tbody = document.getElementById('queryBaseTableBody');
                if (tbody) {
                    tbody.innerHTML = `<tr><td colspan="5" class="empty-state"><div style="color: red;">Error loading data: ${error.message}</div></td></tr>`;
                }
                    if (form) form.reset();
                    document.getElementById('queryOrderModalTitle').textContent = 'New Query Order';
                    document.getElementById('queryOrderId').value = '';
                }
            } else {
                // Reset form for new entry
                if (modalId === 'queryBaseModal') document.getElementById('queryBaseForm').reset();
                if (modalId === 'queryFilterModal') document.getElementById('queryFilterForm').reset();
            console.log('closeModal called:', modalId);
            const modal = document.getElementById(modalId);
            if (modal) {
                modal.classList.remove('active');
            }
            }
        }

        function closeModal(modalId) {
            document.getElementById(modalId).classList.remove('active');
        }

        // Query Base Functions
        async function loadQueryBases() {
            console.log('renderQueryBases called with:', data);
            try {
            if (!tbody) {
                console.error('Table body not found: queryBaseTableBody');
                return;
            }

            if (!data || data.length === 0) {
                const data = await response.json();
                renderQueryBases(data);
            } catch (error) {
                console.error('Error loading query bases:', error);
            tbody.innerHTML = data.map(item => {
                const itemJson = JSON.stringify(item).replace(/"/g, '&quot;');
                return `
            }
                    <td><strong>${escapeHtml(item.nameUnique)}</strong></td>
                    <td>${escapeHtml(item.context)}</td>
                    <td>${escapeHtml(item.description || '-')}</td>
            console.log('renderQueryBases called with:', data);
            const tbody = document.getElementById('queryBaseTableBody');
                        <button class="btn btn-secondary btn-small" onclick='editQueryBase(${itemJson})'>Edit</button>
                console.error('Table body not found: queryBaseTableBody');
                return;
            }
            `;
            }).join('');
            console.log('Table rendered successfully');
            if (!data || data.length === 0) {
                tbody.innerHTML = '<tr><td colspan="5" class="empty-state"><div>No query bases found. Create your first one!</div></td></tr>';
                return;
            }

            tbody.innerHTML = data.map(item => {
                const itemJson = JSON.stringify(item).replace(/"/g, '&quot;');
                return `
                <tr>
                    <td><strong>${escapeHtml(item.nameUnique)}</strong></td>
                    <td>${escapeHtml(item.context)}</td>
                    <td>${escapeHtml(item.description || '-')}</td>
                    <td><span class="badge ${item.active ? 'badge-active' : 'badge-inactive'}">${item.active ? 'Active' : 'Inactive'}</span></td>
                    <td>
                        <button class="btn btn-secondary btn-small" onclick='editQueryBase(${itemJson})'>Edit</button>
                        <button class="btn btn-danger btn-small" onclick="deleteQueryBase('${item.id}')">Delete</button>
                    </td>
                </tr>
            `;
            }).join('');
            console.log('Table rendered successfully');
        }

        function fillQueryBaseForm(data) {
            document.getElementById('queryBaseModalTitle').textContent = 'Edit Query Base';
            document.getElementById('queryBaseId').value = data.id;
            document.getElementById('queryBaseName').value = data.nameUnique;
            document.getElementById('queryBaseContext').value = data.context;
            document.getElementById('queryBaseQuery').value = data.query;
            document.getElementById('queryBaseDescription').value = data.description || '';
            document.getElementById('queryBaseActive').checked = data.active;
        }

        function editQueryBase(data) {
            openModal('queryBaseModal', data);
        }

        async function saveQueryBase(event) {
            event.preventDefault();

            const id = document.getElementById('queryBaseId').value;
            const data = {
                nameUnique: document.getElementById('queryBaseName').value,
                context: document.getElementById('queryBaseContext').value,
                query: document.getElementById('queryBaseQuery').value,
                description: document.getElementById('queryBaseDescription').value,
                active: document.getElementById('queryBaseActive').checked
            };

            try {
                const url = id ? `${API_BASE_URL}/bases/${id}` : `${API_BASE_URL}/bases`;
                const method = id ? 'PUT' : 'POST';

                const response = await fetch(url, {
                    method: method,
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });

                if (response.ok) {
                    closeModal('queryBaseModal');
                    loadQueryBases();
                    showAlert('success', `Query Base ${id ? 'updated' : 'created'} successfully!`);
                } else {
                    showAlert('error', 'Failed to save query base');
                }
            } catch (error) {
                console.error('Error saving query base:', error);
                showAlert('error', 'Failed to save query base');
            }
        }

        async function deleteQueryBase(id) {
            if (!confirm('Are you sure you want to delete this query base?')) return;

            try {
                const response = await fetch(`${API_BASE_URL}/bases/${id}`, { method: 'DELETE' });
                if (response.ok) {
                    loadQueryBases();
                    showAlert('success', 'Query Base deleted successfully!');
                } else {
                    showAlert('error', 'Failed to delete query base');
                }
            } catch (error) {
                console.error('Error deleting query base:', error);
                showAlert('error', 'Failed to delete query base');
            }
        }

        // Query Filter Functions
        async function loadQueryFilters() {
            try {
                const response = await fetch(`${API_BASE_URL}/filters`);
                const data = await response.json();
                renderQueryFilters(data);
            } catch (error) {
                console.error('Error loading query filters:', error);
                showAlert('error', 'Failed to load query filters');
            }
        }

        function renderQueryFilters(data) {
            console.log('renderQueryFilters called with:', data);
            const tbody = document.getElementById('queryFilterTableBody');
            if (!tbody) {
                console.error('Table body not found: queryFilterTableBody');
                return;
            }

            if (!data || data.length === 0) {
                tbody.innerHTML = '<tr><td colspan="5" class="empty-state"><div>No query filters found. Create your first one!</div></td></tr>';
                return;
            }

            tbody.innerHTML = data.map(item => {
                const itemJson = JSON.stringify(item).replace(/"/g, '&quot;');
                return `
                <tr>
                    <td><strong>${escapeHtml(item.nameUnique)}</strong></td>
                    <td>${escapeHtml(item.context)}</td>
                    <td>${escapeHtml(item.description || '-')}</td>
                    <td><span class="badge ${item.active ? 'badge-active' : 'badge-inactive'}">${item.active ? 'Active' : 'Inactive'}</span></td>
                    <td>
                        <button class="btn btn-secondary btn-small" onclick='editQueryFilter(${itemJson})'>Edit</button>
                        <button class="btn btn-danger btn-small" onclick="deleteQueryFilter('${item.id}')">Delete</button>
                    </td>
                </tr>
            `;
            }).join('');
        }

        function fillQueryFilterForm(data) {
            document.getElementById('queryFilterModalTitle').textContent = 'Edit Query Filter';
            document.getElementById('queryFilterId').value = data.id;
            document.getElementById('queryFilterName').value = data.nameUnique;
            document.getElementById('queryFilterContext').value = data.context;
            document.getElementById('queryFilterFragment').value = data.queryFragment;
            document.getElementById('queryFilterDescription').value = data.description || '';
            document.getElementById('queryFilterActive').checked = data.active;
        }

        function editQueryFilter(data) {
            openModal('queryFilterModal', data);
        }

        async function saveQueryFilter(event) {
            event.preventDefault();

            const id = document.getElementById('queryFilterId').value;
            const data = {
                nameUnique: document.getElementById('queryFilterName').value,
                context: document.getElementById('queryFilterContext').value,
                queryFragment: document.getElementById('queryFilterFragment').value,
                description: document.getElementById('queryFilterDescription').value,
                active: document.getElementById('queryFilterActive').checked
            };

            try {
                const url = id ? `${API_BASE_URL}/filters/${id}` : `${API_BASE_URL}/filters`;

                const response = await fetch(url, {
                    method: method,
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });

                if (response.ok) {
                    closeModal('queryFilterModal');
                    loadQueryFilters();
                    showAlert('success', `Query Filter ${id ? 'updated' : 'created'} successfully!`);
                } else {
                    showAlert('error', 'Failed to save query filter');
                }
            } catch (error) {
                console.error('Error saving query filter:', error);
                showAlert('error', 'Failed to save query filter');
            }
        }

            console.log('renderQueryOrders called with:', data);
        async function deleteQueryFilter(id) {
            if (!tbody) {
                console.error('Table body not found: queryOrderTableBody');
                return;
            }

            if (!data || data.length === 0) {

            try {
                const response = await fetch(`${API_BASE_URL}/filters/${id}`, { method: 'DELETE' });
                if (response.ok) {
            tbody.innerHTML = data.map(item => {
                const itemJson = JSON.stringify(item).replace(/"/g, '&quot;');
                return `
                    showAlert('success', 'Query Filter deleted successfully!');
                    <td><strong>${escapeHtml(item.nameUnique)}</strong></td>
                    <td>${escapeHtml(item.context)}</td>
                    <td>${escapeHtml(item.description || '-')}</td>
            } catch (error) {
                console.error('Error deleting query filter:', error);
                        <button class="btn btn-secondary btn-small" onclick='editQueryOrder(${itemJson})'>Edit</button>
            }
        }

            `;
            }).join('');
        async function loadQueryOrders() {
            try {
                const response = await fetch(`${API_BASE_URL}/orders`);
                const data = await response.json();
                renderQueryOrders(data);
            } catch (error) {
                console.error('Error loading query orders:', error);
                showAlert('error', 'Failed to load query orders');
            }
        }

        function renderQueryOrders(data) {
            console.log('renderQueryOrders called with:', data);
            const tbody = document.getElementById('queryOrderTableBody');
            if (!tbody) {
                console.error('Table body not found: queryOrderTableBody');
                return;
            }

            if (!data || data.length === 0) {
                tbody.innerHTML = '<tr><td colspan="5" class="empty-state"><div>No query orders found. Create your first one!</div></td></tr>';
                return;
            }

            tbody.innerHTML = data.map(item => {
                const itemJson = JSON.stringify(item).replace(/"/g, '&quot;');
                return `
                <tr>
                    <td><strong>${escapeHtml(item.nameUnique)}</strong></td>
                    <td>${escapeHtml(item.context)}</td>
                    <td>${escapeHtml(item.description || '-')}</td>
                    <td><span class="badge ${item.active ? 'badge-active' : 'badge-inactive'}">${item.active ? 'Active' : 'Inactive'}</span></td>
                    <td>
                        <button class="btn btn-secondary btn-small" onclick='editQueryOrder(${itemJson})'>Edit</button>
                        <button class="btn btn-danger btn-small" onclick="deleteQueryOrder('${item.id}')">Delete</button>
                    </td>
                </tr>
            `;
            }).join('');
        }

        function fillQueryOrderForm(data) {
            document.getElementById('queryOrderModalTitle').textContent = 'Edit Query Order';
            document.getElementById('queryOrderId').value = data.id;
            document.getElementById('queryOrderName').value = data.nameUnique;
            document.getElementById('queryOrderContext').value = data.context;
            document.getElementById('queryOrderFragment').value = data.queryFragment;
            document.getElementById('queryOrderDescription').value = data.description || '';
            document.getElementById('queryOrderActive').checked = data.active;
        }

        function editQueryOrder(data) {
            openModal('queryOrderModal', data);
        }

        async function saveQueryOrder(event) {
            event.preventDefault();

            const id = document.getElementById('queryOrderId').value;
            const data = {
                nameUnique: document.getElementById('queryOrderName').value,
                context: document.getElementById('queryOrderContext').value,
                queryFragment: document.getElementById('queryOrderFragment').value,
                description: document.getElementById('queryOrderDescription').value,
                active: document.getElementById('queryOrderActive').checked
            };

            try {
                const url = id ? `${API_BASE_URL}/orders/${id}` : `${API_BASE_URL}/orders`;
                const method = id ? 'PUT' : 'POST';

                const response = await fetch(url, {
                    method: method,
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });

                if (response.ok) {
                    closeModal('queryOrderModal');
                    loadQueryOrders();
                    showAlert('success', `Query Order ${id ? 'updated' : 'created'} successfully!`);
                } else {
                    showAlert('error', 'Failed to save query order');
                }
            } catch (error) {
                console.error('Error saving query order:', error);
                showAlert('error', 'Failed to save query order');
            }
        }

        async function deleteQueryOrder(id) {
            if (!confirm('Are you sure you want to delete this query order?')) return;

            try {
                const response = await fetch(`${API_BASE_URL}/orders/${id}`, { method: 'DELETE' });
                if (response.ok) {
                    loadQueryOrders();
                    showAlert('success', 'Query Order deleted successfully!');
                } else {
                    showAlert('error', 'Failed to delete query order');
                }
            } catch (error) {
                console.error('Error deleting query order:', error);
                showAlert('error', 'Failed to delete query order');
            }
        }

        // Query Preview Functions
        async function previewQuery() {
            const baseQuery = document.getElementById('previewBaseQuery').value;
            const filtersText = document.getElementById('previewFilters').value;
            const ordersText = document.getElementById('previewOrders').value;
            const parametersText = document.getElementById('previewParameters').value;

            if (!baseQuery) {
                showAlert('error', 'Base query is required');
                return;
            }

            const filters = filtersText.split('\n').filter(f => f.trim());
            const orders = ordersText.split('\n').filter(o => o.trim());

            let parameters = {};
            if (parametersText) {
                try {
                    parameters = JSON.parse(parametersText);
                } catch (e) {
                    showAlert('error', 'Invalid JSON in parameters');
                    return;
                }
            }

            const requestData = {
                baseQuery: baseQuery,
                filters: filters,
                orders: orders,
                parameters: parameters
            };

            try {
                const response = await fetch(`${API_BASE_URL}/preview`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(requestData)
                });

                const result = await response.json();
                renderPreviewResults(result);
            } catch (error) {
                console.error('Error previewing query:', error);
                showAlert('error', 'Failed to preview query');
            }
        }

        function renderPreviewResults(result) {
            const container = document.getElementById('previewResults');

            let html = '<div class="preview-section">';

            // Final Query
            html += '<h4>📝 Final Query</h4>';
            html += `<div class="code-preview">${escapeHtml(result.finalQuery)}</div>`;

            // Status
            if (result.valid) {
                html += '<div class="alert alert-success">✅ Query is valid!</div>';
            } else {
                html += `<div class="alert alert-error">❌ Query Error: ${escapeHtml(result.errorMessage)}</div>`;
            }

            // Preview Results
            if (result.valid && result.previewResults && result.previewResults.length > 0) {
                html += `<h4>📊 Preview Results (${result.totalResults} total)</h4>`;
                html += '<div class="results-table"><table>';

                // Table header
                const keys = Object.keys(result.previewResults[0]);
                html += '<thead><tr>';
                keys.forEach(key => {
                    html += `<th>${escapeHtml(key)}</th>`;
                });
                html += '</tr></thead>';

                // Table body
                html += '<tbody>';
                result.previewResults.forEach(row => {
                    html += '<tr>';
                    keys.forEach(key => {
                        html += `<td>${escapeHtml(String(row[key]))}</td>`;
                    });
                    html += '</tr>';
                });
                html += '</tbody></table></div>';
            } else if (result.valid) {
                html += '<div class="alert alert-info">No results found</div>';
            }

            html += '</div>';
            container.innerHTML = html;
        }

        // Utility Functions
        function showAlert(type, message) {
            const alertClass = type === 'success' ? 'alert-success' : 'alert-error';
            const icon = type === 'success' ? '✅' : '❌';

            const alert = document.createElement('div');
            alert.className = `alert ${alertClass}`;
            alert.innerHTML = `${icon} ${message}`;
            alert.style.position = 'fixed';
            alert.style.top = '20px';
            alert.style.right = '20px';
            alert.style.zIndex = '10000';
            alert.style.minWidth = '300px';
            alert.style.animation = 'slideIn 0.3s';

            document.body.appendChild(alert);

            setTimeout(() => {
                alert.style.animation = 'slideOut 0.3s';
                setTimeout(() => alert.remove(), 300);
            }, 3000);
        }

        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }

        // Initialize
        document.addEventListener('DOMContentLoaded', () => {
            loadQueryBases();
        });
    </script>
</body>
</html>

